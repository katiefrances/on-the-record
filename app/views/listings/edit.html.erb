
<div class="container-fluid">
    <h1>Edit Listing</h1>

    <%= render 'form', listing: @listing %>

    <%= link_to 'Show', @listings %> |
    <%= link_to 'Back', listings_path %>
    <%= link_to 'Delete', 
             article_path(article), 
                         method: :delete, 
                         data: { confirm: 'Are you sure?' } %> 
</div>